{
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=9.5",
    "yarn": "^1.3.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "scripts/deploy.sh",
    "build:all": "scripts/build-all.sh",
    "start:frontend": "scripts/start-frontend.sh",
    "start:frontend-dev": "export REACT_APP_WS_URL=ws://localhost:8080/feed/ && scripts/start-frontend.sh",
    "build:frontend": "scripts/build-frontend.sh",
    "build:common": "tsc -p packages/common",
    "check:common": "tsc -p packages/common --noEmit",
    "test": "scripts/test.sh"
  }
}
